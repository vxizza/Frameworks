1. instalar o nodeJS (nodejs.org)
2. npm --version (check do nodejs instalado)
3. instal
ar o angular (angular.io)
4. ng verion (chek angular instalado)



#--------- rotinas do git (add no repositorio)
1. git status (verificar situação)
2. se vermelho -> git add .
3. se verde -> git commit -m "nome do commit" (grava na linha)
4. se tudo ok -> git push (atualiza no github)
5. git clone endereço.git . (comando para copiar do github p local)
5.1. npm install (dentro da pasta do projeto p instalar node_modules)

------ PARA REMOVER USUARIO ANTIGO --------
git config --system --unset credential.helper

#------- ANGULAR
ng new nome-projeto (cria um projeto)
ng serve -o (rodar e abrir o projeto - dentro da pasta do projeto)   
ng g c components/nome --skip-tests ( cria um novo componente - na pasta do projeto)


comando para criar varias divs: (.item .item-$>{item $})*12

#----
pwd (caminho em que estamos)

